<UserControl x:Class="Library.Views.SearchOrder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Library.Views"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             MinHeight="100" MinWidth="1030">

    <UserControl.Resources>

        <Style TargetType="Button" x:Key="OkButton">
            <Setter Property="FontFamily" Value="Courier New"/>
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" CornerRadius="4"
                                SnapsToDevicePixels="True">     
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>

                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#F3E8DF"/>
                                <Setter Property="Background" Value="#320261"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
    </UserControl.Resources>

    <Grid>

        <!--Input e botão OK-->
        <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">

            <Border Height="40" Width="400" Margin="30 0 10 0"
                    CornerRadius="10" Background="#F3E8DF">
                <TextBox Height="40" Width="400" FontFamily="Courier New"
                    BorderThickness="0" FontSize="20" FontWeight="Medium"
                    Background="Transparent" Foreground="Black"
                    VerticalAlignment="Center" VerticalContentAlignment="Center"
                    HorizontalAlignment="Left" HorizontalContentAlignment="Left"/>
            </Border>

            <Border Width="50" Height="40" 
                    CornerRadius="10"  Background="#F3E8DF">
                <Button Content="OK" x:Name="Ok_Button" Click="Ok_Button_Click" Style="{StaticResource OkButton}"/>
            </Border>

        </StackPanel>

    </Grid>
    
</UserControl>
