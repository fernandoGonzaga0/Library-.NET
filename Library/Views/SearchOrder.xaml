<UserControl x:Class="Library.Views.SearchOrder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Library.Views"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             MinHeight="100" MinWidth="1030">

    <UserControl.Resources>

        <Style TargetType="Button" x:Key="OkButton">
            <Setter Property="FontFamily" Value="Courier New"/>
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>

                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#F3E8DF"/>
                                <Setter Property="Background" Value="#aaaaaa"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <fa:IconImage Icon="BookOpen" Foreground="#F3E8DF" Height="30" Margin="0 0 10 0"/>
            <TextBlock Text="Library" Foreground="#F3E8DF" VerticalAlignment="Center" FontSize="25" FontFamily="Courier New"/>
        </StackPanel>

        <!--Input e botão OK-->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0 0 0 10">

            <Border Height="30" Width="500" Margin="0 0 10 0"
                    CornerRadius="10" Background="#F3E8DF">
                <TextBox Height="30" Width="500" FontFamily="Courier New"
                    BorderThickness="0" FontSize="20" FontWeight="Medium"
                    Background="Transparent" Foreground="#000000"
                    VerticalAlignment="Center" VerticalContentAlignment="Center"
                    HorizontalAlignment="Left" HorizontalContentAlignment="Left"
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    />
            </Border>

            <Border Height="30"  Width="50"
                    CornerRadius="10"  Background="#F3E8DF">
                <Button Content="OK" x:Name="Ok_Button" 
                       Command="{Binding SearchCommand}" Style="{StaticResource OkButton}" 
                        FontFamily="Constantia" FontSize="20"/>
            </Border>

        </StackPanel>

    </Grid>

</UserControl>