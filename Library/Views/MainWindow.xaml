<Window x:Class="Library.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library"
        xmlns:userControls="clr-namespace:Library.Views"
        xmlns:views="clr-namespace:Library.Views"
        mc:Ignorable="d"
        Title="MainWindow"
        Width="1280" Height="740"
        WindowStartupLocation="CenterScreen" WindowStyle="None" 
        AllowsTransparency="True" Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <!--Retirando espaços brancos restantes na barra inicial-->
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" CaptionHeight="0" ResizeBorderThickness="5"/>
    </WindowChrome.WindowChrome>

    <Border CornerRadius="40" BorderThickness="2" Padding="10">

        <Border.Background>
            <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
                <GradientStop Color="#00002b" Offset="0"/>
                <GradientStop Color="#00002b" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>

            <!--Colunas-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--Coluna 0 (SideBar)-->

            <views:SideBar Grid.Column="0"/>

            <views:MainMenu Grid.Column="1"/>

        </Grid>

    </Border>



</Window>